# Add above `location /` in the site file, then:
#   sudo nginx -t && sudo systemctl reload nginx
#
# Graphic URLs are Laravel routes. Without this, nginx sees /assets as a
# directory (or a .png static rule) and returns 403 instead of PHP.

location ^~ /assets/ {
    try_files $uri /index.php?$query_string;
}
